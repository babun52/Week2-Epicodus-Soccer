<%= render "layouts/errors", :object => @review %>

<%= simple_form_for [@product, @review] do |f| %>
  <%= f.hidden_field :user_id %>
  <%= f.label :review %>
  <%= f.text_area :review %>
  <%= f.submit %>
<% end %>
